services:
  server:
    build: 
      context: .
      dockerfile: DOCKERFILE
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    command: npm run start
    environment:
      - NODE_ENV=production
